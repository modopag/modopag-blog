# Cloudflare Pages — rewrites e redirects.
# Sintaxe: <source> <destination> <status>
#   - status 200 = rewrite (URL não muda no browser, só serve paths internos)
#   - status 301/302 = redirect (URL muda)
#   - :splat captura o wildcard *
#
# Blog hospedado em blog.modopag.com.br (subdomínio dedicado via CF Pages).
# Proxy de imagens do Supabase fica em functions/img/[[path]].ts (Pages Function
# com cache no edge).

# (Nenhuma regra adicional necessária por enquanto.
# Mantemos o arquivo pra documentar o padrão de configuração.)
