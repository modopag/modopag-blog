---
import BaseLayout from '@/layouts/BaseLayout.astro';
import SEOHead from '@/components/SEOHead.astro';
import { getCategories, getPosts } from '@/lib/database';

const categories = await getCategories();
const popularPosts = await getPosts({ limit: 4, published: true, featured: true });
---

<BaseLayout>
  <SEOHead
    slot="head"
    title="Página não encontrada"
    description="A página que você procura não existe ou foi movida. Explore nosso blog para encontrar conteúdos úteis sobre pagamentos e gestão."
    noindex={true}
  />

  <div class="min-h-[60vh] flex items-center justify-center px-4">
    <div class="max-w-2xl mx-auto text-center">
      <!-- Fun illustration -->
      <div class="text-8xl mb-6">🔍</div>

      <!-- Error code -->
      <h1 class="text-6xl md:text-8xl font-bold text-primary-500 mb-4">404</h1>

      <!-- Message -->
      <h2 class="text-2xl md:text-3xl font-semibold text-secondary-500 dark:text-white mb-4">
        Ops! Página não encontrada
      </h2>

      <p class="text-secondary-400 dark:text-secondary-300 mb-8 max-w-md mx-auto">
        A página que você procura pode ter sido movida, removida ou nunca existiu.
        Mas não se preocupe, temos muito conteúdo útil para você!
      </p>

      <!-- Quick links -->
      <div class="flex flex-wrap justify-center gap-3 mb-12">
        <a
          href="/blog/"
          class="px-6 py-3 bg-primary-500 hover:bg-primary-400 text-secondary-500 font-semibold rounded-lg transition-colors"
        >
          ← Voltar ao Blog
        </a>
        <a
          href="https://modopag.com.br"
          class="px-6 py-3 bg-secondary-100 dark:bg-secondary-700 text-secondary-600 dark:text-secondary-200 rounded-lg font-medium hover:bg-secondary-200 dark:hover:bg-secondary-600 transition-colors"
        >
          Ir para modoPAG
        </a>
      </div>
    </div>
  </div>

  <!-- Popular posts section -->
  {popularPosts.length > 0 && (
    <section class="max-w-7xl mx-auto px-4 pb-16">
      <h3 class="text-xl font-semibold text-secondary-500 dark:text-white mb-6 text-center">
        Conteúdos populares que você pode gostar
      </h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {popularPosts.map(post => (
          <a
            href={`/blog/${post.category?.slug}/${post.slug}/`}
            class="block p-4 bg-white dark:bg-secondary-600 rounded-xl border border-secondary-100 dark:border-secondary-500 hover:shadow-lg transition-shadow"
          >
            <div class="text-2xl mb-2">{post.category?.emoji || '📄'}</div>
            <h4 class="font-medium text-secondary-500 dark:text-white line-clamp-2">
              {post.title}
            </h4>
          </a>
        ))}
      </div>
    </section>
  )}

  <!-- Categories -->
  {categories.length > 0 && (
    <section class="max-w-7xl mx-auto px-4 pb-16">
      <h3 class="text-xl font-semibold text-secondary-500 dark:text-white mb-6 text-center">
        Explore por categoria
      </h3>
      <div class="flex flex-wrap justify-center gap-3">
        {categories.map(cat => (
          <a
            href={`/blog/${cat.slug}/`}
            class="px-4 py-2 bg-white dark:bg-secondary-600 border border-secondary-200 dark:border-secondary-500 rounded-full text-sm font-medium text-secondary-600 dark:text-secondary-200 hover:border-primary-500 hover:text-primary-600 dark:hover:text-primary-400 transition-colors"
          >
            {cat.emoji} {cat.name}
          </a>
        ))}
      </div>
    </section>
  )}
</BaseLayout>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
