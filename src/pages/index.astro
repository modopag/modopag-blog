---
import BaseLayout from '@/layouts/BaseLayout.astro';
import SEOHead from '@/components/SEOHead.astro';
import SchemaOrg from '@/components/SchemaOrg.astro';
import { getCategories, getPosts } from '@/lib/database';
import { generateWebSiteSchema, generateOrganizationSchema } from '@/utils/seo';
import { truncate } from '@/utils/helpers';

// Fetch all data in parallel for performance
const [categories, featuredPosts, latestPosts, calculatorPosts] = await Promise.all([
  getCategories(),
  getPosts({ published: true, featured: true, limit: 3 }),
  getPosts({ published: true, limit: 12 }),
  getPosts({ published: true, categorySlug: 'calculadoras', limit: 3 }),
]);

// Get posts for each category (excluding calculadoras)
const categoriesWithPosts = await Promise.all(
  categories
    .filter(cat => cat.slug !== 'calculadoras')
    .slice(0, 3)
    .map(async (category) => {
      const posts = await getPosts({ published: true, categorySlug: category.slug, limit: 4 });
      return { category, posts };
    })
);

const validCategoriesWithPosts = categoriesWithPosts.filter(({ posts }) => posts.length > 0);

// Use featured posts for hero, or fall back to latest
const heroPosts = featuredPosts.length >= 1
  ? featuredPosts
  : latestPosts.slice(0, 3);

const heroPost = heroPosts[0];
const secondaryPosts = heroPosts.slice(1, 3);

const schemas = [generateWebSiteSchema(), generateOrganizationSchema()];

// UTM params
const utmParams = '?utm_source=blog&utm_medium=homepage-cta&utm_campaign=homepage';
---

<BaseLayout>
  <SEOHead
    slot="head"
    title="Blog"
    description="Dicas, guias e calculadoras gratuitas para ajudar você a gerenciar melhor seu negócio. Aprenda sobre pagamentos, taxas de maquininhas e finanças."
  />
  <SchemaOrg slot="head" schema={schemas} />

  <div class="min-h-screen">
    <!-- Hero Section -->
    <section class="relative overflow-hidden bg-gradient-to-b from-white via-gray-50/50 to-white">
      <!-- Subtle grid pattern -->
      <div class="absolute inset-0 opacity-[0.015]" style="background-image: url('data:image/svg+xml,%3Csvg width=\'60\' height=\'60\' viewBox=\'0 0 60 60\' xmlns=\'http://www.w3.org/2000/svg\'%3E%3Cg fill=\'none\' fill-rule=\'evenodd\'%3E%3Cg fill=\'%23000000\' fill-opacity=\'1\'%3E%3Cpath d=\'M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z\'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>

      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-16 md:pt-12 md:pb-24">
        <!-- Editorial tagline -->
        <div class="text-center mb-12 animate-fade-up">
          <span class="inline-flex items-center gap-2 px-4 py-2 bg-black text-white text-sm font-medium rounded-full">
            <span class="w-2 h-2 bg-primary-500 rounded-full animate-pulse"></span>
            Sem rabo preso com ninguém
          </span>
        </div>

        <!-- Main hero grid -->
        {heroPost && (
          <div class="grid lg:grid-cols-12 gap-6 lg:gap-8">
            <!-- Featured post (large) -->
            <div class="lg:col-span-8 animate-fade-up">
              <a
                href={`/blog/${heroPost.category?.slug || 'artigos'}/${heroPost.slug}/`}
                data-astro-prefetch="viewport"
                class="group block relative overflow-hidden rounded-3xl bg-black aspect-[16/10] lg:aspect-[16/9] card-shine"
              >
                {heroPost.featured_image ? (
                  <img
                    src={heroPost.featured_image}
                    alt={heroPost.featured_image_alt || heroPost.title}
                    class="absolute inset-0 w-full h-full object-cover opacity-80 group-hover:opacity-90 group-hover:scale-105 transition-all duration-700 ease-out"
                    loading="eager"
                  />
                ) : (
                  <div class="absolute inset-0 bg-gradient-to-br from-primary-500/80 to-primary-600/80"></div>
                )}

                <!-- Gradient overlay -->
                <div class="absolute inset-0 bg-gradient-to-t from-black via-black/50 to-transparent"></div>

                <!-- Content -->
                <div class="absolute inset-0 p-6 md:p-10 flex flex-col justify-end">
                  <div class="max-w-2xl">
                    <span class="inline-block px-3 py-1 bg-primary-500 text-black text-xs font-semibold rounded-full mb-4">
                      {heroPost.category?.name || 'Artigo'}
                    </span>

                    <h1 class="text-2xl md:text-4xl lg:text-5xl font-bold text-white mb-4 group-hover:text-primary-400 transition-colors duration-300 leading-tight">
                      {heroPost.title}
                    </h1>

                    {heroPost.description && (
                      <p class="text-gray-300 text-base md:text-lg mb-6 line-clamp-2">
                        {truncate(heroPost.description, 150)}
                      </p>
                    )}

                    <div class="flex items-center gap-4">
                      <span class="inline-flex items-center gap-2 text-white font-medium group-hover:text-primary-400 transition-colors">
                        Ler artigo
                        <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                        </svg>
                      </span>
                      {heroPost.reading_time && (
                        <span class="text-gray-400 text-sm">{heroPost.reading_time} min</span>
                      )}
                    </div>
                  </div>
                </div>
              </a>
            </div>

            <!-- Secondary posts -->
            <div class="lg:col-span-4 flex flex-col gap-6">
              {secondaryPosts.map((post, index) => (
                <a
                  href={`/blog/${post.category?.slug || 'artigos'}/${post.slug}/`}
                  data-astro-prefetch="viewport"
                  class="group relative overflow-hidden rounded-2xl bg-black flex-1 min-h-[200px] card-shine animate-fade-up"
                  style={`animation-delay: ${(index + 1) * 100}ms`}
                >
                  {post.featured_image ? (
                    <img
                      src={post.featured_image}
                      alt={post.featured_image_alt || post.title}
                      class="absolute inset-0 w-full h-full object-cover opacity-70 group-hover:opacity-80 group-hover:scale-105 transition-all duration-700 ease-out"
                      loading="lazy"
                    />
                  ) : (
                    <div class="absolute inset-0 bg-gradient-to-br from-gray-800 to-gray-900"></div>
                  )}

                  <div class="absolute inset-0 bg-gradient-to-t from-black via-black/40 to-transparent"></div>

                  <div class="absolute inset-0 p-5 flex flex-col justify-end">
                    <span class="inline-block px-2.5 py-1 bg-white/10 backdrop-blur-sm text-white text-xs font-medium rounded-full mb-3 w-fit">
                      {post.category?.name || 'Artigo'}
                    </span>
                    <h2 class="text-lg font-semibold text-white group-hover:text-primary-400 transition-colors line-clamp-2">
                      {post.title}
                    </h2>
                  </div>
                </a>
              ))}
            </div>
          </div>
        )}
      </div>
    </section>

    <!-- Categories Pills -->
    <section class="border-y border-gray-100 bg-white sticky top-16 z-40">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center gap-2 py-4 overflow-x-auto scrollbar-hide">
          <a
            href="/blog/"
            class="flex-shrink-0 px-4 py-2 bg-black text-white text-sm font-medium rounded-full transition-all hover:bg-gray-800"
          >
            Todos
          </a>
          {categories.map((category) => (
            <a
              href={`/blog/${category.slug}/`}
              class="flex-shrink-0 px-4 py-2 bg-gray-100 text-gray-700 text-sm font-medium rounded-full transition-all hover:bg-gray-200 hover:text-black"
            >
              {category.emoji && <span class="mr-1.5">{category.emoji}</span>}
              {category.name}
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- Latest Posts -->
    <section class="py-16 md:py-24 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-end justify-between mb-12">
          <div>
            <h2 class="text-3xl md:text-4xl font-bold text-black mb-2">
              Últimas publicações
            </h2>
            <p class="text-gray-500">
              Conteúdo fresco direto do forno
            </p>
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {latestPosts.slice(0, 8).map((post, index) => (
            <article class="group animate-fade-up" style={`animation-delay: ${index * 50}ms`}>
              <a
                href={`/blog/${post.category?.slug || 'artigos'}/${post.slug}/`}
                data-astro-prefetch="viewport"
                class="block h-full"
              >
                <div class="relative aspect-[4/3] rounded-2xl overflow-hidden mb-4 bg-gray-100">
                  {post.featured_image ? (
                    <img
                      src={post.featured_image}
                      alt={post.featured_image_alt || post.title}
                      class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-500 ease-out"
                      loading="lazy"
                    />
                  ) : (
                    <div class="absolute inset-0 bg-gradient-to-br from-gray-200 to-gray-300 flex items-center justify-center">
                      <span class="text-4xl">{post.category?.emoji || '📄'}</span>
                    </div>
                  )}

                  <!-- Hover overlay -->
                  <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition-colors duration-300"></div>
                </div>

                <div class="space-y-2">
                  <span class="text-xs font-medium text-gray-500 uppercase tracking-wide">
                    {post.category?.name || 'Artigo'}
                  </span>
                  <h3 class="text-lg font-semibold text-black group-hover:text-primary-600 transition-colors line-clamp-2 leading-snug">
                    {post.title}
                  </h3>
                  {post.reading_time && (
                    <p class="text-sm text-gray-400">{post.reading_time} min de leitura</p>
                  )}
                </div>
              </a>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- Newsletter Section - Minimal & Elegant -->
    <section class="py-20 md:py-28 bg-black text-white relative overflow-hidden">
      <!-- Subtle gradient orbs -->
      <div class="absolute top-0 left-1/4 w-96 h-96 bg-primary-500/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-primary-500/5 rounded-full blur-3xl"></div>

      <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative">
        <h2 class="text-3xl md:text-5xl font-bold mb-4 animate-fade-up">
          Dicas semanais para<br />
          <span class="gradient-text">lucrar mais</span>
        </h2>

        <p class="text-gray-400 text-lg mb-10 animate-fade-up" style="animation-delay: 100ms">
          — sem rabo preso com ninguém
        </p>

        <form
          method="POST"
          action="/"
          data-rptn-form="78682"
          class="flex flex-col sm:flex-row gap-3 max-w-md mx-auto animate-fade-up"
          style="animation-delay: 200ms"
          id="newsletter-form-homepage"
        >
          <input
            data-rptn-field="email"
            type="email"
            name="email"
            placeholder="Digite seu e-mail"
            required
            class="flex-1 px-5 py-4 bg-white/10 border border-white/20 rounded-xl text-white placeholder:text-gray-500 focus:outline-none focus:border-primary-500 focus:bg-white/15 transition-all"
          />
          <button
            type="submit"
            class="px-8 py-4 bg-primary-500 text-black font-semibold rounded-xl hover:bg-primary-400 transition-all hover:scale-[1.02] active:scale-[0.98]"
          >
            Cadastrar
          </button>
        </form>

        <p class="text-xs text-gray-500 mt-4 animate-fade-up" style="animation-delay: 300ms">
          Ao se cadastrar, você concorda com nossa{' '}
          <a href="https://modopag.com.br/privacidade" class="underline hover:text-gray-300 transition-colors">
            Política de Privacidade
          </a>
        </p>

        <!-- Success message -->
        <div id="newsletter-success" class="hidden mt-6 p-4 bg-green-500/20 rounded-xl border border-green-500/30">
          <p class="text-green-400 font-medium">Cadastro realizado com sucesso!</p>
        </div>
      </div>
    </section>

    <!-- Calculators Section -->
    {calculatorPosts.length > 0 && (
      <section class="py-16 md:py-24 bg-gray-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex items-end justify-between mb-12">
            <div>
              <span class="text-sm font-medium text-primary-600 uppercase tracking-wide mb-2 block">Ferramentas</span>
              <h2 class="text-3xl md:text-4xl font-bold text-black">
                Calculadoras gratuitas
              </h2>
            </div>
            <a
              href="/blog/calculadoras/"
              class="hidden sm:inline-flex items-center gap-2 text-sm font-medium text-black hover:text-primary-600 transition-colors underline-animation"
            >
              Ver todas
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </a>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            {calculatorPosts.map((post, index) => (
              <a
                href={`/blog/calculadoras/${post.slug}/`}
                data-astro-prefetch="viewport"
                class="group relative bg-white rounded-2xl p-6 border border-gray-200 hover:border-primary-500 hover:shadow-xl hover:shadow-primary-500/10 transition-all duration-300 hover:-translate-y-1 animate-fade-up"
                style={`animation-delay: ${index * 100}ms`}
              >
                <div class="flex items-start justify-between mb-4">
                  <div class="w-12 h-12 bg-primary-100 rounded-xl flex items-center justify-center group-hover:bg-primary-500 transition-colors">
                    <span class="text-2xl group-hover:scale-110 transition-transform">🧮</span>
                  </div>
                  <span class="px-2.5 py-1 bg-green-100 text-green-700 text-xs font-medium rounded-full">
                    Grátis
                  </span>
                </div>

                <h3 class="text-lg font-semibold text-black mb-2 group-hover:text-primary-600 transition-colors">
                  {post.title}
                </h3>

                {post.description && (
                  <p class="text-gray-500 text-sm line-clamp-2 mb-4">
                    {truncate(post.description, 100)}
                  </p>
                )}

                <span class="inline-flex items-center gap-1 text-sm font-medium text-primary-600 group-hover:gap-2 transition-all">
                  Usar agora
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                  </svg>
                </span>
              </a>
            ))}
          </div>
        </div>
      </section>
    )}

    <!-- CTA Section -->
    <section class="py-16 md:py-24 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="relative overflow-hidden rounded-3xl bg-primary-500 p-8 md:p-16">
          <!-- Decorative elements -->
          <div class="absolute top-0 right-0 w-64 h-64 bg-white/10 rounded-full -translate-y-1/2 translate-x-1/2 blur-2xl"></div>
          <div class="absolute bottom-0 left-0 w-48 h-48 bg-black/5 rounded-full translate-y-1/2 -translate-x-1/2 blur-xl"></div>

          <div class="relative grid lg:grid-cols-2 gap-8 items-center">
            <div>
              <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-black mb-6">
                Aceite pagamentos<br />com as menores taxas
              </h2>

              <p class="text-black/70 text-lg mb-8 max-w-lg">
                A partir de <span class="font-bold text-black">0,99%</span> no débito e crédito. Sem aluguel, sem mensalidade.
              </p>

              <div class="flex flex-wrap gap-4">
                <a
                  href={`https://modopag.com.br/maquininhas${utmParams}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-flex items-center gap-2 px-6 py-3 bg-black text-white font-semibold rounded-xl hover:bg-gray-800 transition-all hover:scale-[1.02] active:scale-[0.98]"
                  data-no-instant
                >
                  Ver maquininhas
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                  </svg>
                </a>

                <a
                  href={`https://modopag.com.br/taxas${utmParams}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-flex items-center gap-2 px-6 py-3 bg-white/50 text-black font-semibold rounded-xl hover:bg-white transition-all"
                  data-no-instant
                >
                  Simular taxas
                </a>
              </div>
            </div>

            <div class="hidden lg:flex justify-center">
              <div class="relative">
                <img
                  src="/blog/images/maquininha-modopag.png"
                  alt="Maquininha modoPAG"
                  class="w-72 h-auto drop-shadow-2xl transform -rotate-6 hover:rotate-0 transition-transform duration-500"
                  loading="lazy"
                  onerror="this.style.display='none'"
                />
                <!-- Fallback illustration -->
                <div class="w-72 h-96 bg-white/30 rounded-3xl flex items-center justify-center" style="display: none;" id="cta-fallback">
                  <span class="text-8xl">💳</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Posts by Category -->
    {validCategoriesWithPosts.map(({ category, posts }, catIndex) => (
      <section class="py-16 bg-white border-t border-gray-100">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex items-end justify-between mb-10">
            <div class="flex items-center gap-3">
              {category.emoji && (
                <span class="text-3xl">{category.emoji}</span>
              )}
              <h2 class="text-2xl md:text-3xl font-bold text-black">
                {category.name}
              </h2>
            </div>
            <a
              href={`/blog/${category.slug}/`}
              class="text-sm font-medium text-gray-500 hover:text-black transition-colors underline-animation"
            >
              Ver todos
            </a>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            {posts.slice(0, 4).map((post, index) => (
              <article class="group">
                <a
                  href={`/blog/${category.slug}/${post.slug}/`}
                  data-astro-prefetch="viewport"
                  class="block"
                >
                  <div class="relative aspect-[4/3] rounded-xl overflow-hidden mb-3 bg-gray-100">
                    {post.featured_image ? (
                      <img
                        src={post.featured_image}
                        alt={post.featured_image_alt || post.title}
                        class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                        loading="lazy"
                      />
                    ) : (
                      <div class="absolute inset-0 bg-gradient-to-br from-gray-200 to-gray-300 flex items-center justify-center">
                        <span class="text-3xl">{category.emoji || '📄'}</span>
                      </div>
                    )}
                  </div>

                  <h3 class="font-medium text-black group-hover:text-primary-600 transition-colors line-clamp-2 leading-snug">
                    {post.title}
                  </h3>
                </a>
              </article>
            ))}
          </div>
        </div>
      </section>
    ))}

    <!-- Final CTA - Minimal -->
    <section class="py-12 bg-gray-50 border-t border-gray-100">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col md:flex-row items-center justify-between gap-6">
          <div class="flex items-center gap-4 text-center md:text-left">
            <span class="text-4xl">💳</span>
            <div>
              <h3 class="text-lg font-bold text-black">
                Comece a aceitar pagamentos hoje
              </h3>
              <p class="text-gray-500 text-sm">
                Taxas a partir de 0,99%. Sem aluguel.
              </p>
            </div>
          </div>

          <a
            href={`https://modopag.com.br/maquininhas${utmParams}`}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 px-6 py-3 bg-black text-white font-semibold rounded-xl hover:bg-gray-800 transition-all whitespace-nowrap"
            data-no-instant
          >
            Conhecer modoPAG
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
</style>

<script>
  function initNewsletterForm() {
    const form = document.getElementById('newsletter-form-homepage');
    const success = document.getElementById('newsletter-success');

    if (!form || !success) return;

    form.addEventListener('submit', () => {
      setTimeout(() => {
        form.classList.add('hidden');
        success.classList.remove('hidden');
      }, 500);
    });
  }

  initNewsletterForm();
  document.addEventListener('astro:after-swap', initNewsletterForm);
</script>
