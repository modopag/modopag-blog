---
import LeadCaptureForm from './LeadCaptureForm.astro';

interface Props {
  variant?: 'standalone' | 'sidebar';
  class?: string;
}

const { variant = 'standalone', class: className = '' } = Astro.props;

const SEGMENT_ID = '78682'; // Newsletter Principal
---

{variant === 'standalone' && (
  <section class:list={['bg-secondary-500 py-16 rounded-2xl', className]}>
    <div class="max-w-3xl mx-auto px-6 text-center">
      <h2 class="text-2xl md:text-3xl font-bold text-white mb-2">
        Dicas semanais para lucrar mais
      </h2>
      <p class="text-secondary-200 mb-8">
        Receba conteudo exclusivo — sem rabo preso com ninguem
      </p>
      <LeadCaptureForm
        segmentId={SEGMENT_ID}
        buttonText="Cadastrar Gratis"
        showName={true}
        showPhone={false}
        variant="inline"
      />
    </div>
  </section>
)}

{variant === 'sidebar' && (
  <div class:list={['bg-white border border-secondary-100 rounded-xl p-6 shadow-sm', className]}>
    <div class="text-center mb-4">
      <span class="text-3xl">💡</span>
    </div>
    <LeadCaptureForm
      segmentId={SEGMENT_ID}
      title="Nao perca nenhuma dica"
      description="Cadastre-se e receba conteudos exclusivos."
      buttonText="Me Cadastrar"
      showName={true}
      showPhone={false}
    />
  </div>
)}
