---
import LeadCaptureForm from './LeadCaptureForm.astro';

interface Props {
  variant?: 'standalone' | 'sidebar';
  class?: string;
}

const { variant = 'standalone', class: className = '' } = Astro.props;

const SEGMENT_ID = '78682'; // Newsletter Principal
---

{variant === 'standalone' && (
  <section class:list={['bg-gradient-to-br from-primary-50 to-primary-100 py-16 rounded-2xl', className]}>
    <div class="max-w-xl mx-auto px-6 text-center">
      <h2 class="text-3xl font-bold text-secondary-500 mb-2">
        Dicas semanais para lucrar mais
      </h2>
      <p class="text-secondary-400 text-lg mb-8">
        — sem rabo preso com ninguém
      </p>
      <div class="max-w-md mx-auto">
        <LeadCaptureForm
          segmentId={SEGMENT_ID}
          buttonText="Cadastrar Grátis"
          showName={false}
          showPhone={false}
        />
      </div>
    </div>
  </section>
)}

{variant === 'sidebar' && (
  <div class:list={['bg-white border border-secondary-100 rounded-xl p-6 shadow-sm', className]}>
    <div class="text-center mb-4">
      <span class="text-3xl">💡</span>
    </div>
    <LeadCaptureForm
      segmentId={SEGMENT_ID}
      title="Não perca nenhuma dica"
      description="Cadastre-se e receba conteúdos exclusivos."
      buttonText="Me Cadastrar"
      showName={false}
      showPhone={false}
    />
  </div>
)}
